load("@bazel_skylib//rules:diff_test.bzl", "diff_test")
load("@rules_zig//zig:defs.bzl", "zig_test")

zig_test(
    name = "test",
    size = "small",
    main = "src/main.zig",
	  srcs = glob(["src/*.zig"]),
		deps = ["@zig_toml//:toml"]
)
